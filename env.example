# ClinicConnect Environment Configuration
# Copy this file to .env and update the values

# ===========================================
# DATABASE CONFIGURATION
# ===========================================
# Local development
DATABASE_URL=postgresql://clinicuser:clinic_dev_2024@localhost:5434/clinicconnect

# For Docker Compose
# DATABASE_URL=postgresql://clinicuser:your-password@db:5432/clinicconnect

# For DigitalOcean Managed Database
# DATABASE_URL=postgresql://doadmin:password@your-cluster.db.ondigitalocean.com:25060/clinicconnect?sslmode=require

# ===========================================
# APPLICATION SETTINGS
# ===========================================
NODE_ENV=development
PORT=5001

# ===========================================
# SECURITY SECRETS (REQUIRED FOR PRODUCTION)
# ===========================================
# Generate secure secrets with: openssl rand -base64 64

# JWT Secret - Used for API token authentication
# REQUIRED IN PRODUCTION: Server will fail to start without this in production mode
# Generate with: openssl rand -base64 64
JWT_SECRET=change-this-to-a-secure-random-string

# Session Secret - Used for cookie-based sessions
# REQUIRED IN PRODUCTION: Sessions will not persist across restarts without this
# Generate with: openssl rand -base64 64
SESSION_SECRET=change-this-to-another-secure-random-string

# ===========================================
# DEVELOPMENT-ONLY SETTINGS (DO NOT USE IN PRODUCTION)
# ===========================================
# Allow demo passwords for development/testing (development only)
# Set to 'true' to enable demo password authentication in development
# SECURITY: Never set this to 'true' in production!
ALLOW_DEMO_PASSWORDS=false

# Demo passwords (comma-separated) - only used if ALLOW_DEMO_PASSWORDS=true
# SECURITY: These are for development/testing only. Never use in production!
# Example: DEMO_PASSWORDS=admin123,doctor123,super123
DEMO_PASSWORDS=

# ===========================================
# DATABASE CREDENTIALS (for Docker Compose)
# ===========================================
DB_USER=clinicuser
DB_PASSWORD=changeme

# ===========================================
# OPTIONAL: AI INTEGRATION
# ===========================================
# OpenAI API Key (for AI-assisted features)
OPENAI_API_KEY=

# Anthropic API Key (Claude AI)
ANTHROPIC_API_KEY=

# ===========================================
# OPTIONAL: EMAIL SERVICE
# ===========================================
# SendGrid API Key (for email notifications)
SENDGRID_API_KEY=

# Email sender address
EMAIL_FROM=noreply@yourdomain.com

# ===========================================
# OPTIONAL: WHATSAPP SERVICE (FREE - RECOMMENDED)
# ===========================================
# Green API - FREE WhatsApp messaging (100 messages/day)
# Sign up at: https://green-api.com
# Get credentials from: https://console.green-api.com
# After creating instance, scan QR code to connect WhatsApp
GREEN_API_ID=
GREEN_API_TOKEN=

# ===========================================
# OPTIONAL: WHATSAPP SERVICE (PAID ALTERNATIVE)
# ===========================================
# Twilio WhatsApp API (paid service)
# Only used if Green API is not configured
# Alternative if you need more than 100 messages/day
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_WHATSAPP_FROM=whatsapp:+14155238886

# ===========================================
# PRODUCTION SETTINGS
# ===========================================
# Trust proxy (for running behind nginx/load balancer)
# TRUST_PROXY=true

